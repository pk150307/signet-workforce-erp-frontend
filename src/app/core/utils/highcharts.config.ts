import * as Highcharts from 'highcharts';

/** Shared Highcharts defaults for the app. */
Highcharts.setOptions({
  chart: {
    style: {
      fontFamily: 'ProximaNova-Regular, sans-serif',
    },
  },
  accessibility: {
    enabled: false,
  },
  credits: {
    enabled: false,
  },
});
